services:
  nginx:
    image: nginx:latest
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      # certbot(호스트)이 검증 파일을 놓는 디렉터리. nginx.conf 의 acme-challenge location 이 읽는다.
      - /var/www/certbot:/var/www/certbot:ro
      - ./.htpasswd:/etc/nginx/.htpasswd:ro
    # (정상/비정상 종료 불문) 항상 재시작 (ex) host reboot -> docker demon init -> nginx start)
    restart: always
    networks:
      - internal

  Prometheus:
    image: prom/prometheus:latest
    container_name: prom-prod
    expose:
      - 9090
    volumes:
      - ./prometheus/prometheus-prod.yml:/etc/prometheus/prometheus.yml
      - ./prom-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.external-url=/htj-prometheus/'
      - '--web.route-prefix=/'
    restart: always
    networks:
      - internal

  Grafana:
    image: grafana/grafana:latest
    container_name: grafana-local
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: ${GF_ADMIN_USER}
      GF_SECURITY_ADMIN_PASSWORD: ${GF_ADMIN_PASSWORD}
      GF_SERVER_ROOT_URL: https://fightweek.kr/htj-grafana/
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"
    volumes:
      - ./grafana-data:/var/lib/grafana
    mem_limit: 700m
    restart: always
    networks:
      - internal

  flask-app:
    image: ${FLASK_ECR_IMAGE}
    container_name: flask-container
    env_file:
      - .env
    # 매일 새벽 3시 크롤링(Spring 이 호출) 때 여기까지 치솟는다.
    ports:
      - "5000:5000"
    mem_limit: 850m

networks:
  internal:
    driver: bridge